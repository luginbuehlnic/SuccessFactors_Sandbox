_schema-version: '3.1'
ID: SuccessFacotor_API_IPA
version: 1.0.0
description: "IPA SuccessFactors API"
parameters:
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npx cds build --production
modules:
  - name: SuccessFacotor_API_IPA-srv
    type: nodejs
    path: gen/srv
    parameters:
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: SuccessFacotor_API_IPA-db
      - name: SuccessFacotor_API_IPA-auth

  - name: SuccessFacotor_API_IPA-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: SuccessFacotor_API_IPA-db

resources:
  - name: SuccessFacotor_API_IPA-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: SuccessFacotor_API_IPA-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: SuccessFacotor_API_IPA-${org}-${space}
        tenant-mode: dedicated
